import { render } from '@testing-library/react-native';
import {{pascalCase name}}Base from '../components/{{pascalCase name}}Base';

describe("{{pascalCase name}} Component", () => {
  it("renders correctly", () => {
    const { getByText } = render(<{{pascalCase name}}Base />);
    expect(getByText("{{pascalCase name}} Component")).toBeTruthy();
  });
});