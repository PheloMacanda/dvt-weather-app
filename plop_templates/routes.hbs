import { Stack } from 'expo-router';

const {{pascalCase name}}Layout = () => {
  return (
    <Stack>
      <Stack.Screen name='index' options={{ title: '{{pascalCase name}}' }} />
    </Stack>
  );
};

export default {{pascalCase name}}Layout;