import { View, Text } from 'react-native';

const Splash = () => {
    return (
        <View className='flex-1 justify-center items-center'>
            <Text>splash home</Text>
        </View>
    );
};

export default Splash;